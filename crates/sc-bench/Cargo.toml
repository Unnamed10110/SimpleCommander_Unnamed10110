[package]
name = "sc-bench"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
sc-core = { path = "../sc-core" }
sc-shell = { path = "../sc-shell" }
sc-index = { path = "../sc-index" }
fastrand = "2.5.0"

[dev-dependencies]
criterion = "0.8.2"

[[bench]]
name = "core_benches"
harness = false
